import React from 'react';

const RightSidebar = ({user}) => {
    return (
        <div className="fixed top-0 z-10 right-0 w-[20%] bg-amber-600 h-screen">
            righ sidebar
        </div>
    );
};

export default RightSidebar;